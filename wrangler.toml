# Cloudflare Pages config for the (still-pending) Hostinger → CF hosting migration.
# Static site only — no functions/bindings. Deploy: npm run build && npx wrangler pages deploy
name = "thaisazevedo"
pages_build_output_dir = "dist"
compatibility_date = "2026-07-01"
